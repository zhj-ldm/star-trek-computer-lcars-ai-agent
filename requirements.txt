# ============================================
# Star Trek computer 项目依赖清单
# 运行环境: Python >= 3.11（推荐 3.11.9）
# ============================================
#
# 安装命令（在项目目录下执行）:
#   pip install -r requirements.txt
#
# 国内网络建议使用清华镜像加速:
#   pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple
#
# 版本号取自当前已验证可用的运行环境（~/agentscope-env）

# ===== 核心依赖（必需） =====
numpy==2.4.6
sounddevice==0.5.5
faster-whisper==1.2.1
agentscope==2.0.6
flask==3.1.3

# ===== 可选依赖（按需加载） =====
edge-tts==7.2.8
openwakeword==0.6.0
searchpin==1.0.8

# ===== 关键间接依赖（由上述库带入，显式列出便于排查） =====
onnxruntime==1.28.0
ctranslate2==4.8.1
huggingface_hub==1.27.0
openai==3.0.0
anthropic==0.122.0
dashscope==1.26.7
scipy==1.17.1
scikit-learn==1.9.0
（内容由AI生成，仅供参考）
